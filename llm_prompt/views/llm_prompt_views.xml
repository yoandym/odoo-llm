<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Form View for LLM Prompt -->
    <record id="llm_prompt_view_form" model="ir.ui.view">
        <field name="name">llm.prompt.form</field>
        <field name="model">llm.prompt</field>
        <field name="arch" type="xml">
            <form string="LLM Prompt">
                <header>
                    <button name="auto_detect_arguments" string="Auto-detect Arguments" type="object" class="oe_highlight" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_toggle" options="{'terminology': 'archive'}" />
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="e.g. Data Analysis Prompt" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="category_id" />
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                        </group>
                        <group>
                            <field name="template_count" readonly="1" />
                            <field name="argument_count" readonly="1" />
                            <field name="usage_count" readonly="1" />
                            <field name="last_used" readonly="1" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description" placeholder="Describe the purpose and usage of this prompt..." />
                        </page>
                        <page string="Templates" name="templates">
                            <field name="template_ids" context="{'default_prompt_id': active_id}">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle" />
                                    <field name="role" />
                                    <field name="content" />
                                    <field name="condition" />
                                </tree>
                            </field>


                            <div class="alert alert-muted mt-3" style="font-size: 0.95em;" role="alert">
                                <strong>What is available?</strong>
                                <ul>
                                    <li>
                                        <b>Template Variables (Jinja2 syntax with {{variable}}):</b>
                                        <ul>
                                            <li><code>{{user.name}}</code>, <code>{{user.email}}</code>, <code>{{user.company}}</code> - Current user info</li>
                                            <li><code>{{env.company}}</code>, <code>{{env.lang}}</code>, <code>{{env.tz}}</code> - Environment context</li>
                                            <li><code>{{thread.name}}</code>, <code>{{thread.model}}</code> - Thread information (if in a thread)</li>
                                            <li><code>{{record.field_name}}</code> - Any field from the related record (e.g., <code>{{record.email}}</code>, <code>{{record.state}}</code>)</li>
                                            <li><code>{{now.date}}</code>, <code>{{now.datetime}}</code>, <code>{{now.weekday}}</code> - Current date/time info</li>
                                            <li><code>{{args.argument_name}}</code> - Access any custom argument</li>
                                            <li><code>{{variable|default('fallback')}}</code> - Use filters: default, upper, lower, title, capitalize, date</li>
                                        </ul>
                                    </li>
                                    <li>
                                        <b>Condition Context (Python expressions):</b>
                                        <ul>
                                            <li><code>arguments</code> - Full dictionary of all arguments</li>
                                            <li><code>args</code> - Custom arguments only (without internal ones)</li>
                                            <li><code>user</code>, <code>env</code>, <code>thread</code>, <code>record</code>, <code>now</code> - Same context objects as templates</li>
                                            <li>Direct argument access: <code>user_role == "admin"</code></li>
                                        </ul>
                                    </li>
                                </ul>
                                <strong>What kind of execution conditions can you use?</strong>
                                <ul>
                                    <li>
                                        <b>User-based conditions:</b>
                                        <code>user['company'] == "ACME Corp"</code>, <code>user['lang'] == "es_ES"</code>
                                    </li>
                                    <li>
                                        <b>Record field checks:</b>
                                        <code>record and record['state'] == "draft"</code>, <code>record and record['amount'] &gt; 1000</code>
                                    </li>
                                    <li>
                                        <b>Time-based conditions:</b>
                                        <code>now['hour'] >= 9 and now['hour'] &lt; 17</code>, <code>now['weekday'] in ['Monday', 'Friday']</code>
                                    </li>
                                    <li>
                                        <b>Argument checks:</b>
                                        <code>'debug' in args and args['debug']</code>, <code>user_role == "manager"</code>
                                    </li>
                                    <li>
                                        <b>Complex logic:</b>
                                        <code>(record and record['country_id']['id'] == 233) or env['lang'] == "en_US"</code>
                                    </li>
                                    <li>
                                        <b>Thread context:</b>
                                        <code>thread and thread['model'] == "sale.order"</code>
                                    </li>
                                </ul>
                                <strong>Amazing things you can achieve</strong>
                                <ul>
                                    <li>
                                        <b>Context-Aware Prompts:</b> Automatically adapt prompts based on the current record, user, or environment.<br/>
                                        <i>Example:</i> "Analyzing {{record.display_name}} (Status: {{record.state|title}}) for {{user.company}}"
                                    </li>
                                    <li>
                                        <b>Multi-Language Support:</b> Show different templates based on user language.<br/>
                                        <i>Example condition:</i> <code>user['lang'] == "es_ES"</code> - Spanish instructions
                                    </li>
                                    <li>
                                        <b>Time-Sensitive Instructions:</b> Provide different guidance based on time of day or day of week.<br/>
                                        <i>Example:</i> Include "after-hours support" template when <code>now['hour'] >= 18</code>
                                    </li>
                                    <li>
                                        <b>Role-Based Prompt Flows:</b> Show specialized templates for different user roles or departments.<br/>
                                        <i>Example:</i> Include financial analysis template only when <code>"account" in user['email']</code>
                                    </li>
                                    <li>
                                        <b>Dynamic Record Analysis:</b> Build prompts that automatically reference current record data.<br/>
                                        <i>Example:</i> "Customer {{record.partner_id.display_name}} has {{record.order_line_count}} items totaling {{record.amount_total}}"
                                    </li>
                                    <li>
                                        <b>Conditional Expertise:</b> Add specialized instructions based on record state or type.<br/>
                                        <i>Example:</i> Only include legal compliance text when <code>record and record['amount_total'] &gt; 10000</code>
                                    </li>
                                    <li>
                                        <b>Smart Defaults:</b> Use Jinja2 filters to handle missing data gracefully.<br/>
                                        <i>Example:</i> "Contact: {{record.email|default('No email provided')}}"
                                    </li>
                                </ul>
                            </div>

</page>
<page string="Arguments" name="arguments">
<div class="alert alert-warning" role="alert" invisible="not undefined_arguments">
    <strong
                >Warning:</strong> The following arguments are used in templates but not defined in the schema:
    <field name="undefined_arguments" readonly="1" class="oe_inline" />
</div>
<group>
    <field name="arguments_json" widget="json_editor" placeholder="{
  'system_prompt': {
    'type': 'string',
    'description': 'The system instructions for the LLM',
    'required': true,
    'default': 'You are a helpful assistant.'
  },
  'user_input': {
    'type': 'string',
    'description': 'The user input to process',
    'required': true
  }
}" />
    <field name="input_schema_json" readonly="1" widget="json_editor" />
</group>
<div class="alert alert-info" role="alert">
    <p>
        <strong>Arguments Schema Format:</strong>
    </p>
    <p
                >Define arguments as a JSON object where each key is the argument name and the value is its schema.</p>
    <p>Each argument schema can include:</p>
    <ul>
        <li>
            <strong
                    >type</strong>: string, number, boolean, etc.</li>
        <li>
            <strong
                    >description</strong>: Human-readable description</li>
        <li>
            <strong
                    >required</strong>: Whether the argument is required (boolean)</li>
        <li>
            <strong
                    >default</strong>: Default value if not provided</li>
        <li>
            <strong
                    >examples</strong>: Array of example values</li>
    </ul>
    <p>Referenced in templates using <strong
                  >{{argument_name}}</strong> placeholders.</p>
</div>
</page>
<page string="Testing" name="testing">
<group>
    <field name="example_args" widget="json_editor" />
</group>
<div class="alert alert-info" role="alert">
    <p
                >Enter example arguments in JSON format to test this prompt.</p>
</div>
<button name="action_test_prompt" string="Test Prompt" type="object" class="oe_highlight" />
</page>
<page string="Providers" name="providers">
<field name="provider_ids" widget="many2many_tags" />
<field name="publisher_ids" widget="many2many_tags" />
</page>
</notebook>
</sheet>
<div class="oe_chatter">
<field name="message_follower_ids" />
<field name="activity_ids" />
<field name="message_ids" />
</div>
</form>
</field>
</record>

<!-- Tree View for LLM Prompt -->
<record id="llm_prompt_view_tree" model="ir.ui.view">
<field name="name">llm.prompt.tree</field>
<field name="model">llm.prompt</field>
<field name="arch" type="xml">
<tree>
<field name="name" />
<field name="category_id" />
<field name="tag_ids" widget="many2many_tags" />
<field name="template_count" />
<field name="argument_count" />
<field name="usage_count" />
<field name="last_used" />
<field name="active" invisible="1" />
</tree>
</field>
</record>

<!-- Search View for LLM Prompt -->
<record id="llm_prompt_view_search" model="ir.ui.view">
<field name="name">llm.prompt.search</field>
<field name="model">llm.prompt</field>
<field name="arch" type="xml">
<search string="Search Prompts">
<field name="name" />
<field name="description" />
<field name="category_id" />
<field name="tag_ids" />
<filter string="Archived" name="inactive" domain="[('active', '=', False)]" />
<group expand="0" string="Group By">
<filter string="Category" name="group_by_category" domain="[]" context="{'group_by': 'category_id'}" />
<filter string="Last Used" name="group_by_last_used" domain="[]" context="{'group_by': 'last_used'}" />
</group>
</search>
</field>
</record>
<!-- Prompts Menu -->
<record id="llm_prompt_action" model="ir.actions.act_window">
<field name="name">Prompts</field>
<field name="res_model">llm.prompt</field>
<field name="view_mode">tree,form</field>
<field name="help" type="html">
<p class="o_view_nocontent_smiling_face">
                Create your first prompt template
</p>
<p>
                Prompt templates help you create reusable messages for LLM interactions.
</p>
</field>
</record>

</odoo>
