# Configuration for Llm Prompt

This page explains how to configure the module after installation.

## System Parameters

The following system parameters can be configured:

* `llm_prompt.parameter1` - Description of what this parameter does and its default value
* `llm_prompt.parameter2` - Description of what this parameter does and its default value

To configure these parameters:

1. Go to Technical > Parameters > System Parameters
2. Find or create the parameter
3. Set the appropriate value

## Security Groups

This module defines the following security groups:

* **Group 1** (`module.group_1`): Users who can...
* **Group 2** (`module.group_2`): Users who can...

To assign users to these groups:

1. Go to Settings > Users & Companies > Users
2. Select a user
3. Under Access Rights, find the section for this module
4. Select the appropriate groups

## Settings Configuration

The module adds the following configuration options to the Settings page:

* **Option 1**: Description of what this option does
* **Option 2**: Description of what this option does

To access these settings:

1. Go to Settings
2. Find the section for this module
3. Configure the options as needed
4. Click Save

## Server Configuration

If there are any server-level configurations (odoo.conf), document them here:

```
[options]
module_specific_option = value
```

## Scheduled Actions

This module sets up the following scheduled actions (cron jobs):

* **Action 1**: Description, default frequency, and how to adjust it
* **Action 2**: Description, default frequency, and how to adjust it

To modify these scheduled actions:

1. Go to Technical > Automation > Scheduled Actions
2. Find the action by name
3. Adjust the frequency as needed

## External Services Configuration

If the module integrates with external services, document the configuration here:

1. API keys or credentials needed
2. Endpoints configuration
3. Authentication setup
4. Testing connection
